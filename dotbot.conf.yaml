- clean: [
    '~'
]

- link: 
    ~/.gitconfig: gitconfig
    ~/.tmux.conf: tmux.conf
    ~/.inputrc: inputrc
    ~/.config/nvim: nvim

- shell:
    - [git submodule update --init --recursive, Installing submodules]
